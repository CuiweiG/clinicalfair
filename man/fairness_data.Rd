% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fairness-data.R
\name{fairness_data}
\alias{fairness_data}
\title{Create a fairness evaluation data object}
\usage{
fairness_data(
  predictions,
  labels,
  protected_attr,
  threshold = 0.5,
  reference_group = NULL
)
}
\arguments{
\item{predictions}{Numeric vector of predicted probabilities or
risk scores (between 0 and 1).}

\item{labels}{Binary integer vector of true outcomes (0 or 1).}

\item{protected_attr}{Character or factor vector identifying the
protected group membership (e.g., race, sex, age group).}

\item{threshold}{Decision threshold for converting probabilities
to binary predictions. Default 0.5.}

\item{reference_group}{Name of the reference (privileged) group.
If \code{NULL}, the group with the highest selection rate is used.}
}
\value{
A \code{fairness_data} object (list) with standardized components:
\code{predictions}, \code{labels}, \code{protected}, \code{threshold}, \code{predicted_class},
\code{reference_group}, \code{groups}, \code{n}, \code{prevalence}.
}
\description{
Bundles predictions, labels, and protected attributes into a
standardized container for fairness analysis.
}
\examples{
set.seed(42)
fd <- fairness_data(
  predictions = runif(200),
  labels = rbinom(200, 1, 0.3),
  protected_attr = sample(c("GroupA", "GroupB"), 200, replace = TRUE)
)
fd

}
