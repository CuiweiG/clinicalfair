% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fairness-metrics.R
\name{fairness_metrics}
\alias{fairness_metrics}
\title{Compute fairness metrics across groups}
\usage{
fairness_metrics(
  data,
  metrics = c("selection_rate", "tpr", "fpr", "ppv", "accuracy", "auc", "brier")
)
}
\arguments{
\item{data}{A \link{fairness_data} object.}

\item{metrics}{Character vector of metrics to compute. Default
computes all available metrics. Options: \code{"selection_rate"},
\code{"tpr"}, \code{"fpr"}, \code{"ppv"}, \code{"npv"}, \code{"accuracy"},
\code{"auc"}, \code{"brier"}.}
}
\value{
A \code{fairness_metrics} object (tibble) with columns:
\code{group}, \code{metric}, \code{value}, \code{ratio} (vs reference group),
\code{difference} (vs reference group).
}
\description{
Calculates a comprehensive set of group-wise and comparative
fairness metrics from a \code{fairness_data} object.
}
\details{
Fairness is assessed by comparing metric values across groups.
A ratio of 1.0 or difference of 0.0 indicates perfect parity.
Common thresholds: ratio in \link{0.8, 1.25} (four-fifths rule,
EEOC guidelines) or difference < 0.05.
}
\examples{
set.seed(42)
fd <- fairness_data(
  predictions = c(runif(100, 0.2, 0.8), runif(100, 0.3, 0.9)),
  labels = c(rbinom(100, 1, 0.3), rbinom(100, 1, 0.5)),
  protected_attr = rep(c("A", "B"), each = 100)
)
fairness_metrics(fd)

}
\references{
Obermeyer Z, et al. (2019). Dissecting racial bias in an
algorithm used to manage the health of populations. \emph{Science},
366(6464):447--453. \doi{10.1126/science.aax2342}
}
